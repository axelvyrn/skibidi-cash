dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.0.0
  qr_flutter: ^4.0.0
  provider: ^6.0.5
  audioplayers: ^5.1.0
  bitcoin_flutter: ^3.0.1 # wrapper for BTC logic (for simplicity)
  bip39: ^1.0.3 # for mnemonic generation
flutter:
  assets:
    - assets/images/
    - assets/sounds/
